<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:buttonStyle">@style/ButtonStyleActivityMain</item>
        <item name="android:backgroundTint">@color/dark_color</item>
        <item name="tint">@color/white</item>
        <item name="android:thumbTint">@color/color_switch</item>
        <item name="android:trackTint">@color/color_switch</item>
        <item name="android:checked">true</item>
    </style>

    <style name="Night.Theme.PlaylistMaker" parent="Base.Theme.PlaylistMaker"/>
</resources>

